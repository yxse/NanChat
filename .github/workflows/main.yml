name: 'publish'

on:
  push:
    branches:
      - main

# This workflow will trigger on each push to the `release` branch to create or update a GitHub release, build your app, and upload the artifacts to the release.

jobs:
  publish-tauri:
    permissions:
      contents: write
    strategy:
      fail-fast: false
      matrix:
        include:
          - platform: 'macos-latest' # for Arm based macs (M1 and above).
            args: '--target aarch64-apple-darwin'
          - platform: 'macos-latest' # for Intel based macs.
            args: '--target x86_64-apple-darwin'
          - platform: 'ubuntu-22.04' # for Tauri v1 you could replace this with ubuntu-20.04.
            args: ''
          - platform: 'windows-latest'
            args: ''

    runs-on: ${{ matrix.platform }}
    steps:
      - uses: actions/checkout@v4

      - name: setup node
        uses: actions/setup-node@v4
        with:
          node-version: lts/*

      - name: install Rust stable
        uses: dtolnay/rust-toolchain@stable
        with:
          # Those targets are only used on macos runners so it's in an `if` to slightly speed up windows and linux builds.
          targets: ${{ matrix.platform == 'macos-latest' && 'aarch64-apple-darwin,x86_64-apple-darwin' || '' }}

      - name: install dependencies (ubuntu only)
        if: matrix.platform == 'ubuntu-22.04' # This must match the platform value defined above.
        run: |
          sudo apt-get update
          sudo apt-get install -y libwebkit2gtk-4.0-dev libwebkit2gtk-4.1-dev libappindicator3-dev librsvg2-dev patchelf
        # webkitgtk 4.0 is for Tauri v1 - webkitgtk 4.1 is for Tauri v2.
        # You can remove the one that doesn't apply to your app to speed up the workflow a bit.

      - name: install frontend dependencies
        run: yarn install # change this to npm, pnpm or bun depending on which one you use.

      - uses: tauri-apps/tauri-action@v0
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          VITE_PUBLIC_NODES_API_KEY: ${{ secrets.NODES_API_KEY }}
          VITE_PUBLIC_WS_URL: 'wss://nodes.nanswap.com/ws/'
          VITE_PUBLIC_SERVER_NOTIFICATIONS_PUBLIC_KEY: 'BB0nIzmF18kyuqsUKVPjqbraKNRDdTDmxIVk0HqP9Uam5iwchOMqDE8HTcdgR0ZPGMMRDbcgYCVWBBou-mYs9uI'
          VITE_PUBLIC_BACKEND: 'https://api.nanchat.com'
          VITE_PUBLIC_REDIRECT_URL: 'https://app.nanchat.com'
          VITE_PUBLIC_GOOGLE_ANDROID_APP_ID: '674827583308-cs17h24lp36a236aluhm8n38nq450r03.apps.googleusercontent.com'
          VITE_PUBLIC_GOOGLE_WEB_CLIENT_ID: '674827583308-fn0dvr1ocr89gk29at7sfbdjn7m6bnk4.apps.googleusercontent.com'
          VITE_PUBLIC_firebase_apiKey: 'AIzaSyAHy_2GatOSvUTcrcyqJrxE_Xy7BbJ54cw'
          VITE_PUBLIC_firebase_authDomain: 'nanwallet-efce5.firebaseapp.com'
          VITE_PUBLIC_firebase_projectId: 'nanwallet-efce5'
          VITE_PUBLIC_firebase_storageBucket: 'nanwallet-efce5.firebasestorage.app'
          VITE_PUBLIC_firebase_messagingSenderId: '137198054925'
          VITE_PUBLIC_firebase_appId: '1:137198054925:web:7ca07d199b5f0c912aaf44'
          VITE_PUBLIC_firebase_measurementId: 'G-1GKPS9V51B'
          VITE_PUBLIC_firebase_vapidKey: 'BCj8VTaYMFBloIppZRfKvCR6wfi3lL_qThfzItI5Bam7pBXJMhgE1pqyK4cIpAwCRG_jRMTBiiPJ3exswub3YZg'
          VITE_PUBLIC_TEAM_ACCOUNT: 'nano_1aotdujz8ypijprua9fkerxr9nifbj8bbq5edgztjif45qr3g6fbd1cxenij'
        with:
          tagName: app-v__VERSION__ # the action automatically replaces \_\_VERSION\_\_ with the app version.
          releaseName: 'App v__VERSION__'
          releaseBody: 'See the assets to download this version and install.'
          releaseDraft: true
          prerelease: false
          args: ${{ matrix.args }}
